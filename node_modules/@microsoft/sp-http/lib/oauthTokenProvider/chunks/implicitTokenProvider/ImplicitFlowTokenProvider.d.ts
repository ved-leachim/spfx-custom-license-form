import { IAadTokenProviderConfiguration } from '../../IAadTokenProvider';
/**
 * Class that wraps the replaces ADALs usage.
 * Shamelessly reuses code snippets from ADAL/MSAL.JS
 * Hoping to deprecate/delete when MSAL is production ready.
 * @internal
 */
export declare class ImplicitFlowTokenProvider {
    private readonly _configuration;
    private readonly _tokenRequestManager;
    private readonly _tokenStorage;
    constructor(configuration: IAadTokenProviderConfiguration);
    /**
     * Performs a retry and drops the login hint.
     * @param resourceUri
     * @param useCachedToken
     */
    getToken(resourceUri: string, useCachedToken?: boolean): Promise<string>;
}
//# sourceMappingURL=ImplicitFlowTokenProvider.d.ts.map