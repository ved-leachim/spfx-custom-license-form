import ServiceKey from '../serviceScope/ServiceKey';
declare global {
    interface Window {
        __spWorker: Worker;
        __spWorkerStarted: boolean;
    }
}
/**
 * The web worker proxy is responsible for instantiating the web worker,
 * and setting up a communication channel between the main thread and the worker thread.
 *
 * @internal
 */
export declare class WebWorkerProxy {
    /**
     * The service key for WebWorkerProxy.
     *
     */
    static readonly serviceKey: ServiceKey<WebWorkerProxy>;
    _workerStarted: boolean;
    private _worker;
    init(setPerformancePropertyCallback: (markerName: string) => void): void;
    private _setWorkerStartedListener;
}
//# sourceMappingURL=WebWorkerProxy.d.ts.map