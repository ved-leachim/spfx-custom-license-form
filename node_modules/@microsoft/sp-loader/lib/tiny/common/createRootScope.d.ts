import { ServiceScope } from '@microsoft/sp-core-library';
import { _IODataPageContextInfo } from '@microsoft/sp-odata-types';
/**
 * Create a root service scope and initialize with SPFx services:
 *   - PageContext
 *   - DynamicDataManager
 *   - HttpClient
 *   - SPHttpClient
 *   - GraphHttpClientContext
 *   - DigestCache
 *
 * @param pageContext - the current SPPageContext
 *
 * @returns - the root scope
 */
export declare function createRootScope(pageContext: _IODataPageContextInfo): ServiceScope;
//# sourceMappingURL=createRootScope.d.ts.map